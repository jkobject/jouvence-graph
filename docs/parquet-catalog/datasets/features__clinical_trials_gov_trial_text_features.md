<!-- generated by scripts/parquet_catalog.py; do not edit -->
# `features__clinical_trials_gov_trial_text_features`

- Layer: `features`
- Status: **canonical present**
- As of: `2026-07-27`
- Canonical URI/pattern: `gs://jouvencekb/main/features/clinical_trials_gov_trial_text_features.parquet`
- Logical rows: **6,092**
- Physical objects: **1**
- Bytes: **38,038,096**
- Schema hash (`arrow-fields-v1`): `a6600aee3b6a53211c798968af5229ada346eb273ffdc5396993b1adb33ba9c0`

## Meaning

ClinicalTrials.gov sidecar keyed by NCT ID or a canonical treatment-edge support key.

**Does not mean:** It is not graph topology, a clinical-trial node layer, participant-level data, or a treatment label.

## Keys and graph contract

- Source-contract key fields: `none declared`
- Candidate/join key fields: `nct_id`
- Uniqueness validation: **uniqueness unvalidated by footer-only catalog collection**
- Foreign keys/linkage: `nct_id`

## Columns

| Column | Arrow dtype | Nullable |
|---|---|---:|
| `nct_id` | `large_string` | `true` |
| `source_feature_key` | `large_string` | `true` |
| `source_text` | `large_string` | `true` |
| `source_field_inventory` | `large_string` | `true` |
| `source_text_hash` | `large_string` | `true` |
| `source_text_length` | `int64` | `true` |
| `brief_title` | `large_string` | `true` |
| `official_title` | `large_string` | `true` |
| `brief_summary` | `large_string` | `true` |
| `detailed_description` | `large_string` | `true` |
| `condition_text` | `large_string` | `true` |
| `intervention_text` | `large_string` | `true` |
| `primary_outcome_text` | `large_string` | `true` |
| `secondary_outcome_text` | `large_string` | `true` |
| `eligibility_criteria` | `large_string` | `true` |
| `why_stopped` | `large_string` | `true` |
| `result_summary_text` | `large_string` | `true` |
| `extraction_version` | `large_string` | `true` |
| `source_dataset` | `large_string` | `true` |
| `source_release` | `large_string` | `true` |
| `license` | `large_string` | `true` |
| `created_at` | `large_string` | `true` |

## Source, release, license and provenance

- Source: ClinicalTrials.gov API v2 with OpenTargets treatment links where applicable
- Release: See row-level/source manifest metadata
- License: See canonical source/license report; downstream redistribution remains subject to source review
- Provenance: [../inventory.json](../inventory.json)

## Layout and checksums

The logical dataset is represented by `1` physical Parquet object(s). Physical shards are packaging, not separate datasets.

| Object | Rows | Bytes | Generation | CRC32C | MD5 |
|---|---:|---:|---|---|---|
| `gs://jouvencekb/main/features/clinical_trials_gov_trial_text_features.parquet` | 6,092 | 38,038,096 | `1785155495259824` | `z4KaGg==` | `bAGG4IPYwtFKy5DSKDRMQA==` |
- catalog_manifest: [../inventory.json](../inventory.json)
- schema_doc: [../../kg_schema_overview.md](../../kg_schema_overview.md)

## Read examples

Requester-pays prerequisite (once public IAM permits it):

```bash
export BILLING_PROJECT='<your-gcp-billing-project>'
LOCAL_DIR='./parquet-catalog-data/features__clinical_trials_gov_trial_text_features'
rm -rf -- "$LOCAL_DIR"
mkdir -p "$LOCAL_DIR"
gcloud storage cp --billing-project="$BILLING_PROJECT" 'gs://jouvencekb/main/features/clinical_trials_gov_trial_text_features.parquet' "$LOCAL_DIR/"
```

PyArrow (GCS credentials/application-default credentials must carry the billing project):

```python
import os
import gcsfs
import pyarrow.dataset as ds
billing_project = os.environ['BILLING_PROJECT']
fs = gcsfs.GCSFileSystem(project=billing_project, requester_pays=billing_project)
paths = sorted(fs.glob('jouvencekb/main/features/clinical_trials_gov_trial_text_features.parquet'))
dataset = ds.dataset(paths, filesystem=fs, format='parquet')
print(dataset.head(5, columns=['nct_id']))
```

DuckDB:

```sql
-- Run after the requester-pays `gcloud storage cp` command above.
SELECT "nct_id" FROM read_parquet('./parquet-catalog-data/features__clinical_trials_gov_trial_text_features/clinical_trials_gov_trial_text_features.parquet') ORDER BY "nct_id" NULLS LAST LIMIT 5;
```

## LaminDB / PyG linkage

This is a sidecar. Join through the declared node/edge/NCT keys; it is not topology unless a separate reviewed exporter explicitly consumes it.

## Caveats and leakage

Trial status, outcomes, interventions and text can reveal held-out treatment evidence; split or mask for treatment prediction.
