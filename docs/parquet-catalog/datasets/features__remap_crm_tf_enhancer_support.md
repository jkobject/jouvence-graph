<!-- generated by scripts/parquet_catalog.py; do not edit -->
# `features__remap_crm_tf_enhancer_support`

- Layer: `features`
- Status: **canonical present**
- As of: `2026-07-27`
- Canonical URI/pattern: `gs://jouvencekb/main/features/remap_crm_tf_enhancer_support.parquet`
- Logical rows: **48,768,788**
- Physical objects: **1**
- Bytes: **2,907,628,799**
- Schema hash (`arrow-fields-v1`): `410c411da7c752c91ee74ecf60af84dbf16c8e945d72ae247e87ced97da94cfa`

## Meaning

ReMap 2022 compact CRM aggregate support/QA features.

**Does not mean:** Not graph topology, not observed binding, and not canonical `tf_binds_enhancer` edge/evidence.

## Keys and graph contract

- Source-contract key fields: `none declared`
- Candidate/join key fields: `support_entity_id`
- Uniqueness validation: **uniqueness unvalidated by footer-only catalog collection**
- Foreign keys/linkage: `enhancer_id; tf_gene_id`

## Columns

| Column | Arrow dtype | Nullable |
|---|---|---:|
| `feature_table` | `string` | `true` |
| `support_entity_type` | `string` | `true` |
| `tf_gene_id` | `string` | `true` |
| `tf_symbol_sample` | `string` | `true` |
| `enhancer_id` | `string` | `true` |
| `enhancer_chromosome` | `string` | `true` |
| `enhancer_start` | `int64` | `true` |
| `enhancer_end` | `int64` | `true` |
| `support_entity_id` | `string` | `true` |
| `support_entity_label` | `string` | `true` |
| `crm_support_rows` | `int64` | `true` |
| `crm_interval_count` | `int64` | `true` |
| `evidence_type` | `string` | `true` |
| `support_semantics` | `string` | `true` |
| `relation_under_review` | `string` | `true` |
| `support_scope` | `string` | `true` |
| `source` | `string` | `true` |
| `source_release` | `string` | `true` |
| `source_url` | `string` | `true` |
| `genome_build` | `string` | `true` |
| `liftover_performed` | `bool` | `true` |
| `aggregation_policy` | `string` | `true` |
| `provenance_caveat` | `string` | `true` |
| `source_task_id` | `string` | `true` |
| `promotion_task_id` | `string` | `true` |
| `source_report` | `string` | `true` |
| `readiness_decision_doc` | `string` | `true` |

## Source, release, license and provenance

- Source: ReMap
- Release: 2022; GRCh38/hg38
- License: See canonical manifest/source terms
- Provenance: [../inventory.json](../inventory.json)

## Layout and checksums

The logical dataset is represented by `1` physical Parquet object(s). Physical shards are packaging, not separate datasets.

| Object | Rows | Bytes | Generation | CRC32C | MD5 |
|---|---:|---:|---|---|---|
| `gs://jouvencekb/main/features/remap_crm_tf_enhancer_support.parquet` | 48,768,788 | 2,907,628,799 | `1785155751509875` | `fo/bVw==` | `n/a` |
- catalog_manifest: [../inventory.json](../inventory.json)
- schema_doc: [../../kg_schema_overview.md](../../kg_schema_overview.md)

## Read examples

Requester-pays prerequisite (once public IAM permits it):

```bash
export BILLING_PROJECT='<your-gcp-billing-project>'
LOCAL_DIR='./parquet-catalog-data/features__remap_crm_tf_enhancer_support'
rm -rf -- "$LOCAL_DIR"
mkdir -p "$LOCAL_DIR"
gcloud storage cp --billing-project="$BILLING_PROJECT" 'gs://jouvencekb/main/features/remap_crm_tf_enhancer_support.parquet' "$LOCAL_DIR/"
```

PyArrow (GCS credentials/application-default credentials must carry the billing project):

```python
import os
import gcsfs
import pyarrow.dataset as ds
billing_project = os.environ['BILLING_PROJECT']
fs = gcsfs.GCSFileSystem(project=billing_project, requester_pays=billing_project)
paths = sorted(fs.glob('jouvencekb/main/features/remap_crm_tf_enhancer_support.parquet'))
dataset = ds.dataset(paths, filesystem=fs, format='parquet')
print(dataset.head(5, columns=['feature_table']))
```

DuckDB:

```sql
-- Run after the requester-pays `gcloud storage cp` command above.
SELECT "feature_table" FROM read_parquet('./parquet-catalog-data/features__remap_crm_tf_enhancer_support/remap_crm_tf_enhancer_support.parquet') ORDER BY "feature_table" NULLS LAST LIMIT 5;
```

## LaminDB / PyG linkage

This is a sidecar. Join through the declared node/edge/NCT keys; it is not topology unless a separate reviewed exporter explicitly consumes it.

## Caveats and leakage

Exclude from default topology and supervised regulatory labels unless a reviewed split policy permits it.
