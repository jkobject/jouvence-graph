<!-- generated by scripts/parquet_catalog.py; do not edit -->
# `edges__gene_interacts_gene`

- Layer: `edges`
- Status: **canonical present**
- As of: `2026-07-27`
- Canonical URI/pattern: `gs://jouvencekb/main/edges/gene_interacts_gene.parquet`
- Logical rows: **7,424,037**
- Physical objects: **1**
- Bytes: **36,907,950**
- Schema hash (`arrow-fields-v1`): `7dee93bb2a4f4d78d9cc253aa6202439ce98e8c3f91477c9258d3b51d5f77fe2`

## Meaning

Keep broad for current OpenTargets interaction because canonical endpoints are gene-level; preserve source-specific evidence metadata and do not project text_span product IDs into protein/transcript/TF/enhancer relations.

**Does not mean:** It is not evidence multiplicity: source-specific support belongs in the matching evidence table.

## Keys and graph contract

- Source-contract key fields: `relation, x_id, y_id`
- Candidate/join key fields: `none declared`
- Uniqueness validation: **uniqueness unvalidated by footer-only catalog collection**
- Foreign keys/linkage: `x_id -> nodes/<x_type>.id; y_id -> nodes/<y_type>.id`
- x_type: `gene`
- y_type: `gene`
- kind: `physical`
- direct: `False`

## Columns

| Column | Arrow dtype | Nullable |
|---|---|---:|
| `x_id` | `string` | `true` |
| `x_type` | `string` | `true` |
| `y_id` | `string` | `true` |
| `y_type` | `string` | `true` |
| `relation` | `string` | `true` |
| `display_relation` | `string` | `true` |
| `source` | `string` | `true` |
| `credibility` | `int64` | `true` |

## Source, release, license and provenance

- Source: See table `source` columns and linked KG schema
- Release: Object generation/update date in this catalog; biological source release may be row-level
- License: Not derivable from Parquet metadata alone; consult source provenance before redistribution
- Provenance: [../../kg_schema_overview.md](../../kg_schema_overview.md)

## Layout and checksums

The logical dataset is represented by `1` physical Parquet object(s). Physical shards are packaging, not separate datasets.

| Object | Rows | Bytes | Generation | CRC32C | MD5 |
|---|---:|---:|---|---|---|
| `gs://jouvencekb/main/edges/gene_interacts_gene.parquet` | 7,424,037 | 36,907,950 | `1785155483887629` | `uOngJA==` | `ToVuKhLC7KIly8SQwHZeHQ==` |
- catalog_manifest: [../inventory.json](../inventory.json)
- schema_doc: [../../kg_schema_overview.md](../../kg_schema_overview.md)

## Read examples

Requester-pays prerequisite (once public IAM permits it):

```bash
export BILLING_PROJECT='<your-gcp-billing-project>'
LOCAL_DIR='./parquet-catalog-data/edges__gene_interacts_gene'
rm -rf -- "$LOCAL_DIR"
mkdir -p "$LOCAL_DIR"
gcloud storage cp --billing-project="$BILLING_PROJECT" 'gs://jouvencekb/main/edges/gene_interacts_gene.parquet' "$LOCAL_DIR/"
```

PyArrow (GCS credentials/application-default credentials must carry the billing project):

```python
import os
import gcsfs
import pyarrow.dataset as ds
billing_project = os.environ['BILLING_PROJECT']
fs = gcsfs.GCSFileSystem(project=billing_project, requester_pays=billing_project)
paths = sorted(fs.glob('jouvencekb/main/edges/gene_interacts_gene.parquet'))
dataset = ds.dataset(paths, filesystem=fs, format='parquet')
print(dataset.head(5, columns=['x_id']))
```

DuckDB:

```sql
-- Run after the requester-pays `gcloud storage cp` command above.
SELECT "x_id" FROM read_parquet('./parquet-catalog-data/edges__gene_interacts_gene/gene_interacts_gene.parquet') ORDER BY "x_id" NULLS LAST LIMIT 5;
```

## LaminDB / PyG linkage

`x_id` and `y_id` resolve against canonical node tables. Eligible biological relations become PyG edge stores; metadata-only relations must remain excluded from default training exports.

## Caveats and leakage

Construct train/validation/test splits before exposing relation or evidence fields. Evidence counts, predicates, scores and source text can leak held-out edge labels.
