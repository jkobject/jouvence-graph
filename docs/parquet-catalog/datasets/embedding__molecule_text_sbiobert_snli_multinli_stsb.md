<!-- generated by scripts/parquet_catalog.py; do not edit -->
# `embedding__molecule_text_sbiobert_snli_multinli_stsb`

- Layer: `embedding`
- Status: **canonical present**
- As of: `2026-07-27`
- Canonical URI/pattern: `gs://jouvencekb/main/embeddings/molecule_text_sbiobert_snli_multinli_stsb.parquet`
- Logical rows: **22,230**
- Physical objects: **1**
- Bytes: **69,504,010**
- Schema hash (`arrow-fields-v1`): `39a4c988b787cc24fe86497333a87f2a3151ec8d55b0932ba153c9009aa082ab`

## Meaning

Accepted learned vector table for canonical `molecule` nodes.

**Does not mean:** Vector similarity is not functional equivalence, causality, or therapeutic evidence.

## Keys and graph contract

- Source-contract key fields: `none declared`
- Candidate/join key fields: `embedding_key, node_id`
- Uniqueness validation: **uniqueness unvalidated by footer-only catalog collection**
- Foreign keys/linkage: `node_id`
- node_type: `molecule`

## Columns

| Column | Arrow dtype | Nullable |
|---|---|---:|
| `embedding_key` | `string` | `true` |
| `node_id` | `string` | `true` |
| `node_type` | `string` | `true` |
| `source_feature_table` | `string` | `true` |
| `source_feature_key` | `string` | `true` |
| `source_feature_hash` | `string` | `true` |
| `modality` | `string` | `true` |
| `embedding_model` | `string` | `true` |
| `embedding_version` | `string` | `true` |
| `embedding_dim` | `int32` | `false` |
| `embedding_dtype` | `string` | `true` |
| `embedding_format` | `string` | `false` |
| `embedding` | `fixed_size_list<element: float>[768]` | `false` |
| `pooling` | `string` | `true` |
| `normalization` | `string` | `true` |
| `preprocessing` | `string` | `true` |
| `input_length` | `int64` | `true` |
| `window_count` | `int32` | `false` |
| `created_at` | `string` | `true` |
| `source_feature_release` | `string` | `true` |
| `provenance` | `string` | `true` |
| `license` | `string` | `true` |
| `citation` | `string` | `true` |

## Source, release, license and provenance

- Source: See table `source` columns and linked KG schema
- Release: Object generation/update date in this catalog; biological source release may be row-level
- License: Not derivable from Parquet metadata alone; consult source provenance before redistribution
- Provenance: [../../kg_schema_overview.md](../../kg_schema_overview.md)

## Layout and checksums

The logical dataset is represented by `1` physical Parquet object(s). Physical shards are packaging, not separate datasets.

| Object | Rows | Bytes | Generation | CRC32C | MD5 |
|---|---:|---:|---|---|---|
| `gs://jouvencekb/main/embeddings/molecule_text_sbiobert_snli_multinli_stsb.parquet` | 22,230 | 69,504,010 | `1785155498633508` | `aldyBQ==` | `OIkFGOuVAE7X/ib4cN2ZBw==` |
- catalog_manifest: [../inventory.json](../inventory.json)
- schema_doc: [../../kg_schema_overview.md](../../kg_schema_overview.md)

## Read examples

Requester-pays prerequisite (once public IAM permits it):

```bash
export BILLING_PROJECT='<your-gcp-billing-project>'
LOCAL_DIR='./parquet-catalog-data/embedding__molecule_text_sbiobert_snli_multinli_stsb'
rm -rf -- "$LOCAL_DIR"
mkdir -p "$LOCAL_DIR"
gcloud storage cp --billing-project="$BILLING_PROJECT" 'gs://jouvencekb/main/embeddings/molecule_text_sbiobert_snli_multinli_stsb.parquet' "$LOCAL_DIR/"
```

PyArrow (GCS credentials/application-default credentials must carry the billing project):

```python
import os
import gcsfs
import pyarrow.dataset as ds
billing_project = os.environ['BILLING_PROJECT']
fs = gcsfs.GCSFileSystem(project=billing_project, requester_pays=billing_project)
paths = sorted(fs.glob('jouvencekb/main/embeddings/molecule_text_sbiobert_snli_multinli_stsb.parquet'))
dataset = ds.dataset(paths, filesystem=fs, format='parquet')
print(dataset.head(5, columns=['embedding_key']))
```

DuckDB:

```sql
-- Run after the requester-pays `gcloud storage cp` command above.
SELECT "embedding_key" FROM read_parquet('./parquet-catalog-data/embedding__molecule_text_sbiobert_snli_multinli_stsb/molecule_text_sbiobert_snli_multinli_stsb.parquet') ORDER BY "embedding_key" NULLS LAST LIMIT 5;
```

## LaminDB / PyG linkage

This is a sidecar. Join through the declared node/edge/NCT keys; it is not topology unless a separate reviewed exporter explicitly consumes it.

## Caveats and leakage

Feature coverage is partial and source-dependent. Fit preprocessing only on the training partition and mask features that directly encode the evaluation target.
