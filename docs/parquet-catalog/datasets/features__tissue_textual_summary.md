<!-- generated by scripts/parquet_catalog.py; do not edit -->
# `features__tissue_textual_summary`

- Layer: `features`
- Status: **canonical present**
- As of: `2026-07-27`
- Canonical URI/pattern: `gs://jouvencekb/main/features/tissue_textual_summary.parquet`
- Logical rows: **11,942**
- Physical objects: **1**
- Bytes: **1,290,803**
- Schema hash (`arrow-fields-v1`): `072e4e1123aae68dcd32db4e101aaaa5fe72ae3dbba51a17c9484a660724221b`

## Meaning

Source-backed text payloads for `tissue` nodes, suitable for retrieval or text feature construction.

**Does not mean:** Text is not a curated label, causal claim, or complete coverage of the node table.

## Keys and graph contract

- Source-contract key fields: `none declared`
- Candidate/join key fields: `feature_key, node_id`
- Uniqueness validation: **uniqueness unvalidated by footer-only catalog collection**
- Foreign keys/linkage: `node_id`
- node_type: `tissue`

## Columns

| Column | Arrow dtype | Nullable |
|---|---|---:|
| `feature_key` | `string` | `true` |
| `feature_table` | `string` | `true` |
| `node_id` | `string` | `true` |
| `node_type` | `string` | `true` |
| `summary_kind` | `string` | `true` |
| `summary_text` | `string` | `true` |
| `source` | `string` | `true` |
| `source_dataset` | `string` | `true` |
| `source_record_id` | `string` | `true` |
| `provenance` | `string` | `true` |
| `license` | `string` | `true` |
| `citation` | `string` | `true` |
| `release` | `string` | `true` |
| `created_at` | `string` | `true` |

## Source, release, license and provenance

- Source: Source is recorded in row-level provenance columns
- Release: See row-level `source_release` and object generation
- License: See row-level `license`; obligations vary by source
- Provenance: [../inventory.json](../inventory.json)

## Layout and checksums

The logical dataset is represented by `1` physical Parquet object(s). Physical shards are packaging, not separate datasets.

| Object | Rows | Bytes | Generation | CRC32C | MD5 |
|---|---:|---:|---|---|---|
| `gs://jouvencekb/main/features/tissue_textual_summary.parquet` | 11,942 | 1,290,803 | `1785155496667378` | `uW2SvA==` | `JHwrwyZUuRWTNXURNePDGQ==` |
- catalog_manifest: [../inventory.json](../inventory.json)
- schema_doc: [../../kg_schema_overview.md](../../kg_schema_overview.md)

## Read examples

Requester-pays prerequisite (once public IAM permits it):

```bash
export BILLING_PROJECT='<your-gcp-billing-project>'
LOCAL_DIR='./parquet-catalog-data/features__tissue_textual_summary'
rm -rf -- "$LOCAL_DIR"
mkdir -p "$LOCAL_DIR"
gcloud storage cp --billing-project="$BILLING_PROJECT" 'gs://jouvencekb/main/features/tissue_textual_summary.parquet' "$LOCAL_DIR/"
```

PyArrow (GCS credentials/application-default credentials must carry the billing project):

```python
import os
import gcsfs
import pyarrow.dataset as ds
billing_project = os.environ['BILLING_PROJECT']
fs = gcsfs.GCSFileSystem(project=billing_project, requester_pays=billing_project)
paths = sorted(fs.glob('jouvencekb/main/features/tissue_textual_summary.parquet'))
dataset = ds.dataset(paths, filesystem=fs, format='parquet')
print(dataset.head(5, columns=['feature_key']))
```

DuckDB:

```sql
-- Run after the requester-pays `gcloud storage cp` command above.
SELECT "feature_key" FROM read_parquet('./parquet-catalog-data/features__tissue_textual_summary/tissue_textual_summary.parquet') ORDER BY "feature_key" NULLS LAST LIMIT 5;
```

## LaminDB / PyG linkage

This is a sidecar. Join through the declared node/edge/NCT keys; it is not topology unless a separate reviewed exporter explicitly consumes it.

## Caveats and leakage

Feature coverage is partial and source-dependent. Fit preprocessing only on the training partition and mask features that directly encode the evaluation target.
