<!-- generated by scripts/parquet_catalog.py; do not edit -->
# `features__molecule_treats_disease_clinical_trial_links`

- Layer: `features`
- Status: **canonical present**
- As of: `2026-07-27`
- Canonical URI/pattern: `gs://jouvencekb/main/features/molecule_treats_disease_clinical_trial_links.parquet`
- Logical rows: **7,804**
- Physical objects: **1**
- Bytes: **398,019**
- Schema hash (`arrow-fields-v1`): `0181a8454c2ceb01c92011cf97a7f8b0a08b08300e2bae2b958de90d05164a22`

## Meaning

Canonical public `molecule_treats_disease_clinical_trial_links` features table.

**Does not mean:** Interpret only according to its columns and linked provenance; it is not an implicit graph relation.

## Keys and graph contract

- Source-contract key fields: `none declared`
- Candidate/join key fields: `nct_id, edge_key`
- Uniqueness validation: **uniqueness unvalidated by footer-only catalog collection**
- Foreign keys/linkage: `edge_key; nct_id`

## Columns

| Column | Arrow dtype | Nullable |
|---|---|---:|
| `edge_key` | `large_string` | `true` |
| `relation` | `large_string` | `true` |
| `x_id` | `large_string` | `true` |
| `x_type` | `large_string` | `true` |
| `y_id` | `large_string` | `true` |
| `y_type` | `large_string` | `true` |
| `predicate` | `large_string` | `true` |
| `study_id` | `large_string` | `true` |
| `text_span` | `large_string` | `true` |
| `source` | `large_string` | `true` |
| `source_dataset` | `large_string` | `true` |
| `source_record_id` | `large_string` | `true` |
| `evidence_score` | `double` | `true` |
| `release` | `large_string` | `true` |
| `nct_id` | `large_string` | `true` |
| `mapping_source` | `large_string` | `true` |
| `mapping_method` | `large_string` | `true` |
| `mapping_confidence` | `large_string` | `true` |
| `source_study_id_raw` | `large_string` | `true` |
| `source_text_span_hash` | `large_string` | `true` |

## Source, release, license and provenance

- Source: Source is recorded in row-level provenance columns
- Release: See row-level `source_release` and object generation
- License: See row-level `license`; obligations vary by source
- Provenance: [../inventory.json](../inventory.json)

## Layout and checksums

The logical dataset is represented by `1` physical Parquet object(s). Physical shards are packaging, not separate datasets.

| Object | Rows | Bytes | Generation | CRC32C | MD5 |
|---|---:|---:|---|---|---|
| `gs://jouvencekb/main/features/molecule_treats_disease_clinical_trial_links.parquet` | 7,804 | 398,019 | `1785155497178520` | `OowvEQ==` | `e47HAf/E0ByPqPMah4rsgw==` |
- catalog_manifest: [../inventory.json](../inventory.json)
- schema_doc: [../../kg_schema_overview.md](../../kg_schema_overview.md)

## Read examples

Requester-pays prerequisite (once public IAM permits it):

```bash
export BILLING_PROJECT='<your-gcp-billing-project>'
LOCAL_DIR='./parquet-catalog-data/features__molecule_treats_disease_clinical_trial_links'
rm -rf -- "$LOCAL_DIR"
mkdir -p "$LOCAL_DIR"
gcloud storage cp --billing-project="$BILLING_PROJECT" 'gs://jouvencekb/main/features/molecule_treats_disease_clinical_trial_links.parquet' "$LOCAL_DIR/"
```

PyArrow (GCS credentials/application-default credentials must carry the billing project):

```python
import os
import gcsfs
import pyarrow.dataset as ds
billing_project = os.environ['BILLING_PROJECT']
fs = gcsfs.GCSFileSystem(project=billing_project, requester_pays=billing_project)
paths = sorted(fs.glob('jouvencekb/main/features/molecule_treats_disease_clinical_trial_links.parquet'))
dataset = ds.dataset(paths, filesystem=fs, format='parquet')
print(dataset.head(5, columns=['edge_key']))
```

DuckDB:

```sql
-- Run after the requester-pays `gcloud storage cp` command above.
SELECT "edge_key" FROM read_parquet('./parquet-catalog-data/features__molecule_treats_disease_clinical_trial_links/molecule_treats_disease_clinical_trial_links.parquet') ORDER BY "edge_key" NULLS LAST LIMIT 5;
```

## LaminDB / PyG linkage

This is a sidecar. Join through the declared node/edge/NCT keys; it is not topology unless a separate reviewed exporter explicitly consumes it.

## Caveats and leakage

Feature coverage is partial and source-dependent. Fit preprocessing only on the training partition and mask features that directly encode the evaluation target.
