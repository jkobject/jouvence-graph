<!-- generated by scripts/parquet_catalog.py; do not edit -->
# `nodes__molecule`

- Layer: `nodes`
- Status: **canonical present**
- As of: `2026-07-27`
- Canonical URI/pattern: `gs://jouvencekb/main/nodes/molecule.parquet`
- Logical rows: **31,007**
- Physical objects: **1**
- Bytes: **1,842,052**
- Schema hash (`arrow-fields-v1`): `c6069b518bb95a97b05fc6198653bd2f262f475cbcd5e247f322ba65a37be0fb`

## Meaning

Canonical molecule registry keyed in the ChEMBL namespace (example `CHEMBL941`).

**Does not mean:** Presence is an identifier/registry assertion, not evidence for any biological relation.

## Keys and graph contract

- Source-contract key fields: `id`
- Candidate/join key fields: `none declared`
- Uniqueness validation: **uniqueness unvalidated by footer-only catalog collection**
- Foreign keys/linkage: `none declared`
- node_type: `molecule`

## Columns

| Column | Arrow dtype | Nullable |
|---|---|---:|
| `id` | `large_string` | `true` |
| `legacy_type` | `large_string` | `true` |
| `legacy_id` | `large_string` | `true` |
| `legacy_index` | `double` | `true` |
| `source` | `large_string` | `true` |
| `ncbi_gene_id` | `large_string` | `true` |
| `hgnc_id` | `double` | `true` |
| `uniprot_id` | `double` | `true` |
| `gene_name` | `large_string` | `true` |
| `drugbank_id` | `large_string` | `true` |
| `pubchem_cid` | `large_string` | `true` |
| `cas_rn` | `large_string` | `true` |
| `inchikey` | `large_string` | `true` |
| `smiles` | `large_string` | `true` |
| `mondo_id` | `large_string` | `true` |
| `efo_id` | `double` | `true` |
| `mp_id` | `double` | `true` |
| `mesh_id` | `double` | `true` |
| `hp_id` | `large_string` | `true` |
| `omim_id` | `double` | `true` |
| `doid_id` | `double` | `true` |
| `icd10_code` | `double` | `true` |
| `go_id` | `large_string` | `true` |
| `kegg_id` | `double` | `true` |
| `bto_id` | `double` | `true` |
| `fma_id` | `double` | `true` |
| `name` | `large_string` | `true` |
| `description` | `large_string` | `true` |
| `drug_type` | `large_string` | `true` |
| `is_approved` | `bool` | `true` |
| `max_clinical_trial_phase` | `large_string` | `true` |
| `has_been_withdrawn` | `bool` | `true` |
| `black_box_warning` | `bool` | `true` |

## Source, release, license and provenance

- Source: See table `source` columns and linked KG schema
- Release: Object generation/update date in this catalog; biological source release may be row-level
- License: Not derivable from Parquet metadata alone; consult source provenance before redistribution
- Provenance: [../../kg_schema_overview.md](../../kg_schema_overview.md)

## Layout and checksums

The logical dataset is represented by `1` physical Parquet object(s). Physical shards are packaging, not separate datasets.

| Object | Rows | Bytes | Generation | CRC32C | MD5 |
|---|---:|---:|---|---|---|
| `gs://jouvencekb/main/nodes/molecule.parquet` | 31,007 | 1,842,052 | `1785155478709145` | `gGE+YQ==` | `Sxyi0r/yrO/Gap01XMawZw==` |
- catalog_manifest: [../inventory.json](../inventory.json)
- schema_doc: [../../kg_schema_overview.md](../../kg_schema_overview.md)

## Read examples

Requester-pays prerequisite (once public IAM permits it):

```bash
export BILLING_PROJECT='<your-gcp-billing-project>'
LOCAL_DIR='./parquet-catalog-data/nodes__molecule'
rm -rf -- "$LOCAL_DIR"
mkdir -p "$LOCAL_DIR"
gcloud storage cp --billing-project="$BILLING_PROJECT" 'gs://jouvencekb/main/nodes/molecule.parquet' "$LOCAL_DIR/"
```

PyArrow (GCS credentials/application-default credentials must carry the billing project):

```python
import os
import gcsfs
import pyarrow.dataset as ds
billing_project = os.environ['BILLING_PROJECT']
fs = gcsfs.GCSFileSystem(project=billing_project, requester_pays=billing_project)
paths = sorted(fs.glob('jouvencekb/main/nodes/molecule.parquet'))
dataset = ds.dataset(paths, filesystem=fs, format='parquet')
print(dataset.head(5, columns=['id']))
```

DuckDB:

```sql
-- Run after the requester-pays `gcloud storage cp` command above.
SELECT "id" FROM read_parquet('./parquet-catalog-data/nodes__molecule/molecule.parquet') ORDER BY "id" NULLS LAST LIMIT 5;
```

## LaminDB / PyG linkage

`id` links to pertdb.Compound. PyG uses this table to define the node store; it does not create adjacency.

## Caveats and leakage

Identifiers and ontology hierarchy may encode target structure. Keep split policy explicit and do not infer missing biological assertions from registry membership.
